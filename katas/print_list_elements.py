def print_list_elements(items):
    """
    Prints each element of the list on a new line.
    """


# Example usage
elements = ["apple", "banana", "cherry"]
print_list_elements(elements)
# Expected output:
# apple
# banana
# cherry

"""
To complete this exercise:
--------------------------
Implement the 'print_list_elements' function to iterate over a list and print each element on a new line.

Exercise Breakdown:
-------------
In Python, a `for` loop can be used to iterate over each element in a list. The loop variable (in this case, `item`) 
takes on the value of each element in the list one at a time. 


items = [1, 2, 3, 4, 5]
for item in items:
    ...
"""
