import random

def lottery_guess(guess):
    """
    Simulates a lottery game where the user guesses a single integer.
    The function generates a random integer between 1 and 10, and checks
    if the user's guess matches the lottery number.

    True if the guess matches the lottery number, False otherwise.
    """


user_guess = 5  # Example input
print(lottery_guess(user_guess))  # Example output: True/False, depending on the generated random number


"""
To complete this exercise:
--------------------------
Search how to generate a random number using the imported `random` module.


Exercise Breakdown:
-------------------
Built-in modules like `random` provide convenient functionalities for various programming tasks.
Other useful built-in modules include `math` for mathematical operations and `datetime` for handling date and time.
"""
